# orbital_surfingpikachu

How to use:

- have the app Expo Go installed on iPhone

```
git clone
npx expo install expo-sqlite
npx expo start

```
- scan QR code that is generated with your iPhone
- open the app